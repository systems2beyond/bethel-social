---
description: Manually deploy the application to Netlify production
---

This workflow manually deploys the current state of the application to the Netlify production environment. Use this if automatic deployments via git push are failing or delayed.

1. Run the Netlify deployment command
// turbo
2. netlify deploy --prod
